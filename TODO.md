# TODO

The plan: open work only. The most important thing here is a rule, not a list: **`v1` is closed to new features and ships bug fixes only**, so anything that adds a capability waits for `2.0.0`, whose trigger is a date in the author's life rather than a release number. Contents: that rule and the short `v1` backlog under it, the standing rules every future column or field is measured against, maintenance tied to no release, and the `2.0.0` cluster, the bulk of this file, centered on a ground-up rewrite to Hono + Nuxt on Cloudflare. Shipped work belongs in [`CHANGELOG.md`](CHANGELOG.md); the settled decisions-not-to-build (dark mode, document version history, client-side error tracking, email verification) are in [`notes/HISTORY.md`](notes/HISTORY.md) § Settled, not shipped.

**North star: be the best career app for its one loyal user: myself (the author).** Portfolio value follows from that, not the other way round: a reviewer can tell a tool with a real user from a feature showcase. It governs sequencing: items are ordered by **when in the user's life they pay off**, which is why the `positions` entity triggers on accepting an offer rather than on finishing a prior release, and why the worst-day operations work (backups, the account export, the abuse throttles) was built ahead of everything that would have demoed better. That work is done. What is left to govern is the waiting.

**`v1.11.1` (2026-07-28) is the last tag `v1` will carry, and the next tag is `2.0.0`** *(decided 2026-08-03; `SPEC.md` § Versioning & releases has the reasoning and the one thing it costs)*. Work still lands, it just stops being tagged: a deploy is a `bin/deploy` run, not a tag, so a patch tag over a shipped fix was ceremony the freeze made pointless. [`CHANGELOG.md`](CHANGELOG.md)'s untagged section is the inventory of everything that has landed since, the knowing exceptions to the freeze included; this file deliberately keeps no second count of it, having already carried a stale one. What is still open below: one hardening item with an unsettled home, a dated refresh, what the network security audit of 2026-08-20 left open (`SPEC.md` § Network exposure holds what it verified), the container review's one remaining item, **the findings of the 2026-09-24 code review, which are next**, and the `2.0.0` cluster behind its trigger. The self-hosting review's four follow-ups are closed as of 2026-08-21; `CHANGELOG.md` holds them.

**One standing promise outranks everything here on the day it breaks:** `/privacy` and `/terms` name `karirkalyan@cypherpunkzero.com` (`web/app/lib/links.ts:13`) as the address an erasure request goes to; the mailbox was verified to receive mail 2026-07-12. If that routing breaks, the legal pages are lying, so fixing it outranks anything else in this file that day.

---

## The rule: `v1` takes bug fixes only

**`v1.11.0` was the last feature release, and `v1.11.1` the last release of any kind: `v1` now ships untagged bug fixes and nothing else.** Three reasons, and the first decides it:

- **A `v1` feature is work with a scheduled deletion date.** `2.0.0`'s centerpiece is a ground-up rewrite onto Cloudflare (below): Rails becomes Hono, Next becomes Nuxt. Anything built in `api/` or `web/` from here has to be built a second time in a stack that does not exist yet, so it is paid for twice and demoed once.
- **The app is finished for the search that is actually running.** The north star is one user, and he has the tool: capture, pipeline, follow-ups, takeability constraints, the daily driver. Every remaining idea here pays off *after* the search closes, which is what the `2.0.0` trigger encodes.
- **Cadence.** Each PR burns two CI runs, and each merge adds a manual `bin/deploy` run to make (`CLAUDE.md` § Branching & PRs). A frozen feature surface is what makes the one-PR-per-day rule cheap instead of something to work around.

**The admission test, applied to anything proposed for `v1`:** does it make something the app already does work correctly, or stop something it already does from being abused? If yes it ships, and it is a patch by the mechanical test (no migration, no `v1` contract broken, no new capability). If it adds a capability, however small and however tempting, it is `2.0.0` work and waits for the trigger. Security fixes, dependency bumps, and CI chores pass on the same reading: none is a capability.

**Four exceptions have been taken, knowingly. The first is dashboard pins (2026-08-04).** Up to three applications hoisted to the top of the dashboard list, held in `localStorage` (`SPEC.md` § Pinned applications). It is a capability and it **fails the admission test above**; it was built because the author asked for it with the trade in front of him, not because an argument was found that let it through. It is recorded here so the freeze stays honest: a rule with an unrecorded exception is a rule nobody believes the second time. What it cost is bounded and worth naming, because that boundedness is the whole of its defense: no migration, no endpoint, no Rails file, and `web/`-only, which is also the code the rewrite deletes wholesale, so it is the *most* throwaway shape a feature can have. **This is not a precedent, and the test is unchanged.** The next feature is measured against the test, not against this paragraph.

**The second is the T1 type swap (2026-08-05).** The whole app moves from Fraunces + Manrope + IBM Plex Mono to Plus Jakarta Sans alone ([`BRAND.md`](BRAND.md) § Shipping T1). It adds no capability, so it does not fail the test the way pins did, but it does not pass it either, because only part of it is correctness. The defect is narrow: display-cut italic Fraunces was rendering at 20px in the dashboard and board card titles, where the joins go hairline and the italic `a` and `e` close up. Replacing three typefaces is a larger move than that defect required. The argument for the rest is that the brand book had already made the decision, and a mirror that disagrees with its source is the exact drift this project keeps paying for. Bounded the way pins were: `web/`-only, no migration, no endpoint, no Rails file, and `globals.css` is code the rewrite deletes wholesale. The same sentence applies: not a precedent, and the test is unchanged.

**The third is the screening success rate (2026-08-19).** A fourth stat card, and a fourth figure in the dashboard payload: the percent of applied applications that got past the 書類選考, which is `response_rate` with the rejections taken out (`SPEC.md` § The dashboard payload). It is a new capability and it **fails the admission test**, and it was asked for with the cost in this paragraph in front of the author. One thing separates it from the two above: it is the only exception so far that pays off *during* the search rather than after it, which is the second bullet of the rule met head-on. Response rate alone cannot tell a targeting problem (nobody is reading it) from a resume problem (they read it and said no), and those two have opposite remedies, so a search running right now is exactly when the figure is worth having. Its cost is also worse than the other two, which is worth naming rather than glossing: pins and T1 were both defended on being `web/`-only, the code the rewrite deletes wholesale, and this one is not. It touches `DashboardController`, the payload, `STATS_CACHE_VERSION`, the response schema and both locales, so Hono pays for it a second time. There is no migration: it is two counts over `timeline_entries`, which already hold the answer. **Not a precedent, and the test is unchanged.**

**The fourth is the stat cards on the board (2026-08-28).** The four figures the dashboard already computes (response rate, screening success rate, ghost rate, average time in stage) now render above the Kanban columns as well, read from the same `GET /api/v1/dashboard` payload (`SPEC.md` § Board view). It puts a capability on a surface that did not have it, so it **fails the admission test**, and it was asked for with the cost stated first. Its defense is the one pins and T1 had and the screening rate could not claim: it is `web/`-only. No Rails file changes, no endpoint, no payload field, no `STATS_CACHE_VERSION` bump, no migration. The whole of it is one shared component, two page edits and a message namespace, all of which the Hono + Nuxt rewrite deletes wholesale. It is also the cheapest shape a feature can have, a second reader for a number already computed and already cached. One cost is worth naming rather than glossing: the board now makes a fourth request per render, and a page that previously failed only on its own data now carries a section that can fail on someone else's, which is why the cards hide instead of taking the board down with them. **Not a precedent, and the test is unchanged.**

**The freeze does not gate the `2.0.0` cluster; it is what fills it.** What is frozen is shipping features into the current stack, not wanting them. An idea that arrives tomorrow gets written down there, not argued into a patch.

### Open on `v1`

- [ ] **Fix the findings of the 2026-09-24 code review: this is the next work.** The review ran after the self-hosted deploy went live and covered API security, API correctness and performance, `web/`, and the deploy and CI setup. Every item below fixes existing behavior or stops abuse, so each one passes the admission test. The lessons for the rewrite are in the `2.0.0` cluster under § Carry into the rewrite.
  - **In progress on branch `fix/review-findings-2026-09-24`** (one PR, per the cadence rule):
    - Follow-up reminder rows restart the stage clock. `FollowUpReminderJob` writes a `from_status == to_status` row, and four readers take `MAX(timeline_entries.created_at)` as the date the stage began (`GhostRiskQuery`, `ListQuery::LAST_STAGE_AT`, `DashboardController#avg_days_in_stage`, `ApplicationsController#days_in_stage`). An application with a reminder is flagged as ghosted weeks late.
    - The per-IP throttles are one global bucket. The `web` route handlers call Rails without the client IP, so `req.ip` is the `web` container for every visitor, and five bad sign-ins a minute lock everyone out.
    - The demo account can kill the `api` container with memory. Bulk reads select both PDF blob columns (`ListQuery`, the CSV export, the reminder jobs), the account archive holds every blob plus the zip in memory, and `Demo::ResetService` calls `destroy!`, which loads every blob. A full demo account then fails the hourly reset every hour.
    - The demo's daily AI caps reset every hour, because the reset re-creates the demo user with a new id and the throttles key on the JWT `sub`.
    - Uploads near 1 MB fail with a 413 from the Server Action body limit, and the new-application form is lost.
    - The Anthropic client uses the gem's 600 s timeout and 2 retries inside a 2-thread Puma. `TalkingPointsService` also does not rescue Anthropic errors, so they become a bare 500.
    - Push subscription endpoints are not validated (a blind SSRF from the home host), and `InterviewReminderJob` does not exclude the demo account.
    - A `RecordInvalid` (for example a transition note over 2,000 characters) returns a 422 without the `{ error, code }` envelope.
    - Also taken into this PR because they sat in the same files: sign-out fails while Rails is down and allows a double click; `apiFetch` has no timeout and throws on a network error; the CSV formula escape misses tab and CR.
  - **Second PR, in progress on branch `chore/deploy-backups-ci`:** `bin/deploy` with commit-tagged images, a health wait, a public-hostname check, automatic rollback and pruning; `postgres:18.6`; `pids_limit`; SHA-pinned CI actions with read-only tokens; Playwright on pull requests; a weekly dependency audit; Next.js 16.3.6.
  - **Still open from that PR's scope:**
    - Encrypt the backup dumps with `age` before upload. They hold every resume and password hash. Then run the restore drill again: the last one (2026-07-11) predates the self-hosted runner.
    - Alerting: Honeybadger uptime checks on both hostnames, check-ins from both reminder jobs and `backup.yml`, a disk-space check, and `HONEYBADGER_REVISION` from `bin/deploy`.
    - Turn on GitHub vulnerability alerts (alerts only, no version-update PRs). It is a repo setting, not a file.
  - **Then, medium items:**
    - An SMTP failure loses a follow-up digest for good: the claim is spent before `deliver_later`, and the mail job has no `retry_on`.
    - `Push::Notifier::TRANSIENT_ERRORS` misses `EOFError`, `Errno::ETIMEDOUT` and others, so one network error stops the daily loop for every later user.
    - URL prefill fetches from the home host, so any submitted URL learns the residential IP. At least turn off URL mode on the demo, and correct the "no residential IP exposed" line in `SPEC.md` § Deployment.
  - **Low items, batch into whichever PR is open:** Devise `paranoid` mode; one demo sign-out logs out every demo visitor (shared JTI); `applications.url` accepts any scheme; cursor pagination has no `id` tiebreaker; agency names match case-sensitively while `SPEC.md` says the opposite; a link to `/applications/new?url=` auto-runs prefill from any site (check `Sec-Fetch-Site`); the `[id]` route param is not checked as numeric; an expired session during a list filter shows "filter failed"; the detail page fetches the application after `/transitions` instead of in parallel; the demo button stays disabled after a network error; the board card button says "menu" to screen readers; no `(app)/error.tsx` or `global-error.tsx`; the locale switcher drops the query string; concurrent push subscribe returns 500; `@types/node ^26` against Node 24; `x-powered-by` and `x-middleware-rewrite` response headers; the `NEXT_LOCALE` cookie lacks `Secure`; `SPEC.md` omits `/hsp-calculator` from `OPEN_PATHS` and says `sw.js` handles two events, not three.

- [ ] **Parse API responses against a schema at the `apiFetch` boundary** *(`web/app/lib/api.ts`, whose header points here)*. `v1.11.1` made `ApiResult<T>.data` honestly `T | null` after the `null as T` casts hid a real dashboard crash; `body as T` on the JSON path is the remaining dishonesty, asserting a shape it never checks, so a contract change arrives as an `undefined` deep inside a component instead of an error at the boundary. Correctness rather than capability, so it passes the admission test, and it is the largest piece of pure hardening `v1` has left. **Weigh its home before starting:** the rewrite replaces this file wholesale, so a schema layer built here is thrown away with it. Judge whether the crash class is worth buying twice when it next bites, not now.

- [ ] **Re-confirm the perishable market facts, due July 2027.** The standing rule below requires it and nothing else in the repo schedules it, which under a freeze makes this the one item that comes due on a calendar rather than on a bug report. Three sourced constants, each with a dated comment: `Visa::COE_LEAD_TIME_DAYS` (63, rounded from the MOJ's 62.9-day figure for 技術・人文知識・国際業務, a monthly-published sheet, verified 2026-07-21), `web/app/lib/hsp.ts`'s point table and thresholds (verified against the MOJ and ISA primary sources 2026-07-21), and `Agency::OWNERSHIP_WINDOW_MONTHS` (18, researched 2026-07-11, a convention rather than a published number). A reading pass and at most a constant edit, and a fix by the admission test: a career tool stating last year's rules is stating something false.

- [ ] **One finding left open by the network security audit** *(audited 2026-08-20; `SPEC.md` § Network exposure records the posture it verified and is the place to read before touching any of this)*. The audit changed no application code, and the one fix it made (four cutover-created secret files sitting at mode 664, now 600) needed none. This is the one it could not close, of three. The other two closed on 2026-09-24: Next.js 16.3.6 ships a patched `sharp`, and the code review that day covered the demo reset path (PR #116 fixed what it found). Not urgent and not a capability, so it passes the admission test as hardening.
  - **Cloudflare's edge configuration is the one layer in front of the origin with no committed record anywhere.** WAF rules, rate limiting, Bot Fight Mode: all of it is console state, invisible to this repo and to anyone reading it, which also means an accidental change to it leaves no diff. It matters more than it would on a PaaS, because the tunnel makes Cloudflare the *only* path to the origin, so its config is not a layer of defense in depth here, it is the perimeter. Worth at minimum writing the current settings down beside § Network exposure, so the next reader can tell a deliberate posture from a default.

- [ ] **One finding left open by the 2026-09-17 container review** *(`SPEC.md` § Network exposure records what it closed; `notes/HISTORY.md` has the full entry)*. Hardening, not a capability.
  - **No container runs a read-only root filesystem.** `read_only: true` with `tmpfs` for `/tmp`, `/rails/tmp` and Next's `.next/cache` is the shape; it needs a local run of the stack to find every other path each process writes before it can ship.
- [ ] **Prune this machine's other compose projects, which still publish Postgres and Redis on `0.0.0.0`** *(found 2026-08-20 by the audit above, and not a `karirkalyan` bug)*. The three stopped containers holding those bindings (`hamachi`, `kalima-db-1`, `tarik-redis-1`) were removed the same day, with their named volumes deliberately left intact, so no data was touched. That is hygiene and not a fix: the bindings live in those projects' own compose files, so a `docker compose up` in any of them republishes a database on every interface. `ufw`'s default-deny covered them on *this* machine only while the engine ran inside Docker Desktop's VM; since the 2026-09-01 move to native Docker Engine, a published port bypasses `ufw` (§ Network exposure), so a `docker compose up` in any of them is a LAN-reachable database. The 2026-09-17 review found exactly that with `devdb`, the shared development cluster, and bound it to `127.0.0.1`. Recorded here only because the audit that found it was this project's; the fix belongs in those three repos.

Nothing else is open on `v1`. A bug found tomorrow joins this list; a feature does not.

---

## Standing rules

Three rules that outlive the releases that produced them. The freeze retires the first two from `v1` duty (no patch adds a column or a field), so their live consumer is the `2.0.0` schema work and the record of why the existing columns are shaped as they are; other documents cite them here by name.

- **The trap that breaks a minor whenever it adds a column: the column must be nullable or defaulted.** A `NOT NULL` column with no default means the previous image's `INSERT`s fail against the new database, which by the mechanical test quietly turns a minor into a **major**: the only way a minor violates its own versioning. `v1.6.0`'s two new tables (`credentials`, `push_subscriptions`), `v1.8.0`'s eight `applications` columns, and `v1.9.0`'s visa/hiring/timezone fields all shipped clean under it. A column left loose is deferred, not forgotten: the `2.0.0` schema re-optimization item below owns the tightening pass, so the debt has an address.
- **Field admission test, for any new per-application column** (channel, agency, comp structure, language level, hiring entity, timezone, all of them): it must be **captured at prefill time by `UrlPrefillService`, or cost near-zero manual entry**. The app has one user, and a field he stops filling in after the fifth application is dead schema plus form friction. Bites next on `2.0.0`'s `positions` entity, the residents-only column, and the tightening pass.
- **Perishable-facts refresh rule, for every Japan-market item.** Each embeds perishable external facts: fee schedules, processing times, survey medians. A career tool that confidently states last year's rules is worse than one that says nothing, so each such item must state its **annual refresh cost** when it is scoped, and the sum of those lines is a real cap on how many a solo maintainer can ship. The shipped ones carry their cost already (the `holidays` gem is one `bundle update`; `Visa::COE_LEAD_TIME_DAYS` and the HSP point table are sourced constants). Governs the `2.0.0` career-intelligence item next, which already budgets one data-entry session a year.

---

## Maintenance: not a release

Items that carry no release tag on purpose. The dev-server leak's stopgap already shipped (`cf7cd8d`: 8 GB heap + heap-snapshot flag live in `web/package.json`), and what remains is the upstream report, which **no longer needs to wait for a snapshot** (see the next-actions below). (The jemalloc remove-or-keep item was settled on the remove side 2026-07-20: no measurable win over two measurement windows, so the `Dockerfile` went back to glibc; the record is the chore PR and the next release's `CHANGELOG.md` entry.)

- [ ] **`next dev` crashes even with a 4 GB heap** (`--max-old-space-size=4096`, added in v1.1.1). **Diagnosed 2026-07-11:** a **V8 heap leak in the `next-server` process**, not native/Rust memory and not the OS. Journal shows zero OOM-killer and zero systemd-oomd kills: 30 days at diagnosis, **re-verified over 60 days on 2026-08-03**. **Beware the obvious grep, which reads the exact opposite way.** `journalctl | grep -iE 'out of memory|oom-kill|oomd'` returns **293 hits and zero events**, because the `systemd-oomd` unit's own description is "Userspace Out-Of-Memory (OOM) Killer", so every start, stop, socket, and accounting line repeats the phrase; the count factors as 8 lifecycle line-types across 33 boots, plus 29 `Consumed … CPU time` and `Deactivated successfully` lines. The command that actually answers the question is `journalctl -k | grep -cE 'oom-kill:|Killed process'`, which returns **0**. An instrumented run (`--report-on-signal` diagnostic reports + RSS sampling) showed V8 `usedMemory` tracking RSS growth ~1:1 while the native baseline stayed a flat ~0.8–1.5 GB. Retention is monotonic and unbounded: ~239 MB V8 heap at boot, ~541 MB after warming the ten public routes, then **~40–60 MB retained per HMR rebuild** (3.1 GB used / 5.8 GB RSS after 30 scripted edits, `detachedContextCount` 0 throughout). Raising `--max-old-space-size` only postpones the `JavaScript heap out of memory` abort: 4 GB dies in a normal editing session; 8 GB (the machine has **~60 GB**, `MemTotal` 63,510,216 kB) buys a longer one, and physical RAM was never the binding constraint anyway, since the abort is a V8 ceiling. Matches upstream discussion **#93451** (unbounded growth after HMR edits, ~7 GB, still open and unresolved as of 2026-08-03); issue #85290 (404 → infinite compile loop) does **not** reproduce here. Warm boot-to-ready is ~5 s with the Turbopack FS cache, so a periodic restart is a viable stopgap.
  - **Status 2026-08-03: quiet, not fixed.** No `*.heapsnapshot` exists anywhere in the repo, so the near-heap-limit trigger has not fired once since `cf7cd8d` (2026-07-11). That is the expected reading, not a surprise: the ceiling doubled, retention is per-HMR-rebuild at ~40–60 MB, and almost no front-end editing happened in that window, so zero crashes follows without a single byte of the leak being fixed. It will return on the first day of real `web/` work. (A live server sampled that day sat at 826 MB RSS five minutes after boot, already above the ~541 MB warm baseline.)
  - **Next actions: file upstream now; stop waiting for the snapshot.** The stopgap is **done** (`cf7cd8d`), and the dev script runs with an 8 GB heap and `--heapsnapshot-near-heap-limit=1` (allowed there; `--trace-gc` is not), so a real crash still writes the snapshot. But the blocker was misjudged: a contributor on #93451 is asking reporters to split the problem into **initial-compile peak, idle baseline, and unbounded HMR growth**, and the 2026-07-11 instrumentation already answers all three with numbers (239 MB at boot, 541 MB warm, ~40–60 MB retained per rebuild, `detachedContextCount` 0, native baseline flat at 0.8–1.5 GB, plus zero OS kills over 60 days). That is more than anything in the thread. File it against #93451, and attach the snapshot later as a follow-up. Restart the server when it gets slow. One unexplained datum for that report: V8 heap also jumped ~1.6 GB across the 404 phase and a 45 s idle window, so renders retain memory too, not just compiles (every route here is dynamic, via the CSP-nonce `await connection()`); the three-family font setup compiling into every layout pass may be a multiplier (production loads five variable slices, not ~15 static files).
  - **The rewrite is not the fix, and must not be treated as one.** Nuxt/Nitro is a different dev server with a different leak profile, so this outlives the freeze either way: a daily annoyance with a working stopgap until the trigger fires, and afterwards the upstream report is still the only thing that helps anyone else.

- [ ] **Push notification delivery has never verified.** No `PushSubscription` row was ever created (confirmed empty against api logs and the table itself) during the Railway-to-self-hosted cutover verification (2026-08-20); the browser-side `pushManager.subscribe()` call is failing before it ever reaches the API (the UI's catch-all `pushFailed` message swallows the real error). Non-critical: `FollowUpReminderJob` fires the email digest and the push digest off the same claim, so the same event still reaches the user by mail either way. Worth a browser-console read at some point in case it is a config issue rather than a one-off browser quirk.

---

## `2.0.0` and beyond: triggered by accepting an offer

**This is where every future capability lives.** The freeze closed `v1` to features, so this cluster is not a "someday" appendix: it is the plan, and the rest of the file is the small print of keeping the current app correct until it starts. The trigger is a date in the author's life rather than a release number: **the day he starts his next job.** `CLAUDE.md` carries the same guardrail from the other side, as a standing instruction not to begin any of it early.

KarirKalyan's long-term identity is a **career growth tracker**, not just an application tracker; the name already says so (karir = career, so the product is currently narrower than its own name). **The centerpiece, decided 2026-07-22, is a ground-up rewrite onto Cloudflare: the Rails API becomes a Hono app on Workers, the Next.js frontend becomes Nuxt.** The app stops being a portfolio piece built to impress screeners and becomes the author's own career pet project, rebuilt in his specialty (Vue/Nuxt); everything else in this cluster is what that rewritten app then holds. The `positions` entity is what makes the cluster a **major**: it adds a table *and* changes what `accepted` means in the FSM, and it is the only item in this file that fails the mechanical test.

### The centerpiece: rewrite to Hono + Nuxt on Cloudflare *(item one the moment the search succeeds)*

Decided 2026-07-22. What used to read as three scattered items (a Nuxt "north star", a Hono backend, a move to $0 hosting) is **one move**: it settles three bets at once and they share a single trigger.

- **Specialty signal (the portfolio bet).** Depth over breadth: rather than staying one more generic Next.js tracker, lean all the way into the framework the author already contributes to, so the flagship app *is* the flagship of a Vue/Nuxt-specialist-and-OSS-contributor identity.
- **$0 hosting (the cost bet).** Cloudflare + Hono + Nuxt is the target $0 host: both halves land on Cloudflare's free tier by construction. The self-hosted Docker stack that replaced Railway on 2026-08-20 (`SPEC.md` § Deployment) is a transition to that target, not the target itself. It removed the Railway bill, and its running cost is small because the host is a mini PC with low power draw. But it still runs on the author's own machine: uptime depends on that machine, deploys are a manual `bin/deploy`, and backups need a self-hosted runner beside it. The rewrite removes both the bill and the machine. The always-warm-demo rationale (the repo's job is Tokyo hiring signal; its first hit is a screener clicking the link) expires the moment the job lands, and cost wins.
- **Serverless modernization.** Cold starts stop mattering once nobody is being impressed by a live demo, so the trade-offs that would hurt a portfolio piece are free here.

The target stack, verified free as of 2026-07-22:

- **Backend: Rails → Hono on Cloudflare Workers.** ActiveRecord is replaced by a TS query layer (Drizzle or Kysely over `pg`), so the schema survives but the ORM does not. Solid Queue's persistent poller moves to Cloudflare Cron Triggers. The two invariants port as *shape*, not as a rewrite of the rules: `ApplicationFSM::TRANSITIONS` stays the single copy of the transition table (now in TS), and status changes still route through one transition service, never direct attribute writes.
- **Frontend: Next → Nuxt.** The single largest work item in this file: all of `web/`, the i18n navigation wrapper, the CSP-nonce `proxy.ts` guard, every component and form. Nuxt/Nitro's **native Cloudflare preset** makes the deploy trivial and is the cleaner of the two paths, versus keeping Next behind the OpenNext `@opennextjs/cloudflare` adapter.
- **Data: Postgres stays, the host changes.** Neon or Supabase free tier, fronted by **Cloudflare Hyperdrive** (connection pooler + query cache: free, no egress charge, `node-postgres` its recommended driver, so the classic serverless connection-exhaustion problem is exactly what it absorbs). Two rejected paths, recorded so they are not re-litigated: **D1 is SQLite, not Postgres**, so it is no drop-in for this schema; and Cloudflare's *native* managed Postgres (via PlanetScale, billed through the Cloudflare account, June 2026) is **paid**, the convenience path rather than the free one.
- **Public domain: `kk.chairulakmal.com`** (decided 2026-07-22), bound to the Worker as a custom-domain route. A subdomain of the personal apex on purpose: it puts the author's name as the primary information in the link, so a screener sees whose project this is before the page loads, the signal a standalone `karirkalyan.com` would throw away. It is free and consistent with the $0 goal (one DNS record on a zone already run), and the terse `kk` beats a self-documenting `karirkalyan.` slug precisely to keep the name the dominant token. **Prerequisite, and the one thing that inverts the signal:** `chairulakmal.com` itself must resolve to a real personal site or CV, because a screener will strip `kk.` to see who the author is, and a parked or 404 apex reads worse than no custom domain at all. A dedicated product domain is warranted only on a pivot to external users, which the north star rejects.

**Why the trigger and not now:** the current search targets *any* role, so through it the Next app is the right call, matching the most reqs and already demoing; a rewrite mid-search trades applying-time for a half-migrated flagship that demos worse than the working one. The moment the job lands that flips and this is item one. **What can start now anyway:** casual Nuxt/Vue OSS contributions carry the specialist signal on their own, so only the flagship rewrite waits. **Versioning:** a rewrite that keeps the schema is not automatically a major, so this is not itself the `2.0.0`; it sits in the cluster because its trigger is the same date. The `positions` entity forces the major.

### Carry into the rewrite: lessons from the `v1` code review

The 2026-09-24 review found these as `v1` bugs or weak spots. `v1` gets a patch for each (§ Open on `v1`), but the rewrite should be built so the bug class cannot happen at all. Read this list before the Hono schema and the Nuxt data layer are designed.

- **Store files outside the row.** Put resumes and cover letters in R2 and keep only a key and a size in Postgres. `v1`'s `bytea` columns made every `SELECT *` load megabytes, and Workers have a 128 MB memory limit, so the `v1` shape cannot work there at all. Stream the account zip; never build it in memory.
- **Never `select *` on a table with large columns.** Drizzle and Kysely both make an explicit column list easy. Use it by default.
- **Forward the client IP on purpose.** A Nuxt-to-Hono service binding does not carry `CF-Connecting-IP`. Pass it in a header and have the API trust that header only when the request comes from the binding. `v1`'s throttles became one global bucket because the `web` hop dropped the IP.
- **Key rate limits on an identity that survives a demo reset.** The hourly reset gave the demo user a new id, so every per-account daily cap reset hourly. Give the demo account a stable identity, and keep uploads and AI calls off it or on a much smaller budget.
- **Per-session tokens, not one JTI per user.** One demo sign-out logged out every demo visitor. Require re-authentication to enroll a passkey, and revoke passkeys on a password reset.
- **One fetch wrapper with timeouts and error mapping, for both the Nuxt server and outbound calls.** Every upstream call gets a timeout, a network error becomes a typed failure instead of a thrown exception, and the Anthropic client gets an explicit timeout and retry count. Define the request body limit and the file size limit from one constant.
- **Validate every outbound URL the server will call:** prefill targets and Web Push endpoints alike. On Workers, outbound fetches also stop exposing a home IP.
- **Stage dates count only rows that changed the stage.** A reminder row is not a stage change. Encode this once, in one query helper, instead of four copies of the same `MAX(created_at)`.
- **Cron Triggers:** read `event.scheduledTime` instead of "now", use a "crossed since the last run" rule instead of exact-day matching (a skipped day must not lose a reminder), and send a heartbeat from each handler so a trigger that stops firing is noticed.
- **Delete with `ON DELETE CASCADE`,** not by loading rows to destroy them.
- **Migrations are separate from deploys.** `wrangler rollback` rolls back code, never the schema, so keep migrations expand-then-contract.
- **Backups stay off-provider and encrypted.** Neon or Supabase point-in-time recovery is held by the provider. Keep an `age`-encrypted `pg_dump` from a GitHub Action over a direct connection (not through Hyperdrive).
- **CI holds deploy rights after the move,** so SHA-pin every action and set least-privilege `permissions:` before `CLOUDFLARE_API_TOKEN` enters any workflow.
- **Retry mail and push deliveries after the idempotency claim,** or a transient error loses the reminder for good.

### The rest of the cluster

- [ ] **`positions` (tenure) entity: the keystone; design it before building anything below.** *(the only major in this file)* Today `accepted` is a terminal status; a career tracker needs the job you then held as a first-class thing: company, title, start/end, and the comp structure `v1.8.0` shipped (normalized 年収). Comp history across positions (benchmarkable against the TokyoDev medians cited below), resume versioning, internal-promotion stages, and 職務経歴書 generation all hang off it: a 職務経歴書 is a positions table rendered as prose. Write it into SPEC.md's data model **first**; retrofitting later means migrating what `accepted` means. **Scoping trigger: accepting an offer**, not finishing a prior release: the moment the search succeeds is the moment this entity is needed, or the app exits the user's life exactly when its retention story was supposed to begin.
- [ ] **Make the app survive its own success** *(every sub-item hangs off `positions`)*. Today the lifecycle ends at `accepted`: the search closes and there is no reason to open the app again until the next one. Extend it into the tenure _between_ searches, so landing a job is a state change, not an exit:
  - **Resume as a living document**: the resume is already stored per application; add a current, versioned profile that keeps improving while employed, so the next application starts from the best version rather than a year-old PDF. Feeds the generation item below.
  - **Low-noise market watch**: `wishlist` already models "interesting, not applied"; let it be the employed-mode default. Tracking relevant roles while happily employed is how you know your market value and when to move.
  - **Skills & certs against the market, not a wishlist**: `UrlPrefillService` already reads postings with Claude; extract the skills and certifications each tracked role asks for at prefill time, and the gap against what the profile holds becomes a ranked "learn next" list (JLPT level, cloud certs, a framework) with zero extra data entry. Goals can target the current employer too: an internal promotion has stages like an application does.
  - **Periodic check-ins**: a recurring job and a mailer exist (whatever they are called after the rewrite); a quarterly "update your resume, review your goals" nudge is one more of them, with the same dead-zone awareness as the calendar-aware follow-ups in `v1.4.0`.

  This repositions the tracker from a search tool you abandon on success into a career companion with a retention story, and it is the better portfolio argument too, since every sub-item reuses machinery that already exists (blob storage, `wishlist`, prefill extraction, recurring jobs) rather than demanding new infrastructure.

  **Scope boundary:** career growth does **not** mean drifting into a **networking CRM**, a **habit tracker**, or a **learning tracker**. Those are separate products with mature incumbents, and a broad-shallow app is a worse portfolio piece than a narrow-deep one. The admission test for any career-growth feature: **it must reuse the FSM, the timeline, the prefill extraction, or the `positions` entity**; if it needs none of them, it belongs in a different app. Concretely: contacts go no further than the `agencies` table (which models submission ownership, not relationships); skills/certs/JLPT go no further than the prefill-derived gap list (a ranked "learn next", never course progress or streaks); check-ins stay a recurring nudge, never a journaling habit loop.

- [ ] **`companies` and `company_groups`: make the employer a thing rather than a string** *(wanted 2026-08-04; design it alongside `positions`, ship it with or before)*. `applications.company` is a `string, null: false` today, so every fact about an employer that outlives a single application has nowhere to live: it gets retyped, or lost. Three such facts are wanted first, and they are the ones the search actually turns on. **The reapplication window:** how long after a rejection this employer will look at you again, commonly six or twelve months in Japan, and sometimes shared across a whole group. **The language situation of the team you would actually sit in:** a different fact from `japanese_level`, which records what the *posting demands*; this records what the team runs on day to day, meaning standup and code-review language, and whether the English is one accommodating manager or the whole floor. **What kind of company it is at all:** whether the row is the employer you would work for, or a 派遣会社 or SES vendor that employs you and places you in someone else's office. All three are learned in interviews rather than read off postings, which is precisely why they need an employer-shaped home instead of another `applications` column.
  - **Shape: `companies`, one-to-many under a nullable `company_groups`.** `companies` mirrors `agencies` (per-user, unique on `(user_id, name)`, nullable FK from `applications`), with `applications.company_id` replacing the string, and `positions.company_id` pointing at the same table rather than duplicating the employer as free text a second time. The group is nullable because most employers are not in one, and it exists to answer exactly one question: *I was rejected by X, does that touch Y?* Group-wide reapplication windows and shared ATSes are real in Japanese conglomerates, so the window lives on both levels, the company's own value falling back to its group's, and the board says so when a sibling is already on it.
  - **Company kind (事業会社 vs 客先常駐) is unrecorded anywhere in `v1`, and it is a company fact, which is the sharpest argument for the table.** Nothing today comes close to it: `channel` (`direct`/`agent`/`referral`) says how the posting reached the user, `agency_id` says who owns the submission, and `hiring_entity` says how a Japan resident can be employed at all. A 客先常駐 posting reads perfectly ordinary on all three. Propose the enum on the axis that decides a career rather than the one that decides a contract, meaning whose product you build and who directs your work: `employer` (事業会社, its own product), `haken` (派遣, the vendor employs you and the client directs you, with the 3-year rule and the rest of what 派遣法 attaches), `ses` (準委任 / 客先常駐, the vendor holds the contract and you still sit at the client), `jutaku` (受託開発, client projects done at the vendor's own office). **Whether `haken` and `ses` stay separate is the one call to make when scoping:** the legal difference is 指揮命令権 and it is real, but the career difference between them is thinner than the legal one, and a two-value 事業会社 / 客先 split may carry the whole signal at a quarter of the taxonomy.
    - **Not a fifth `hiring_entity` value.** That column asks whether a company can employ a Japan resident and through what vehicle, aimed at foreign employers with no Japan entity; a 派遣会社 answers `own_entity`, truthfully and uselessly. Folding the two repeats the sponsorship-versus-residency collapse the 2026-08-03 prefill fix removed, one layer down.
    - **It interacts with the other two facts, which is why they want one row.** A 派遣会社 has no meaningful reapplication window, since it takes you back whenever it has a seat, and its language situation is not its own but the client's. So for `haken` and `ses` the English-friendliness field is honestly unanswerable at company level, and the UI should say that rather than collect a lie. *Which* client the user actually sat at is a `positions` fact, because a tenure at one of these firms is a sequence of placements, not one job.
    - **Prefill can usually tell, and must be allowed to say it cannot.** The tells are in the text (派遣, 準委任, 客先常駐, プライム, 二次請け), and the loudest one is negative space: 大手IT企業でのシステム開発 that never names the company. Same rule as `sponsorship`, for the same reason it was learned the hard way: a wrong guess here is worse than an empty field, so unrecorded stays the default and the tells go in the tool-schema description explicitly.
    - **`agencies` and `companies` will sometimes name the same firm, and that is fine.** Persol and Recruit run 人材紹介 and 派遣 arms at once, so one real-world company can be an `agencies` row (it submitted the user somewhere) and a `companies` row (it would employ them). Keep the tables separate: `agencies` exists for submission ownership and is a name vocabulary, so merging them buys a harder migration in exchange for a coincidence. Revisit only if `company_groups` turns out to want agencies as members.
  - **The window is computed, not entered twice.** `rejected → applied` is already an FSM edge and `timeline_entries` already dates the rejection, so "eligible again on <date>" is a query over machinery that exists, plus a warning when a reapplication is opened early and a nudge when the window opens. Reusing the FSM and the timeline is how this clears the career-growth scope boundary above.
  - **Auto-create the company when the application is created**, from the name `UrlPrefillService` already extracts. That is what keeps the common path inside the field admission test: no new form field, no new prefill work. **The cost is deduplication, and it is the real work:** "Google Japan", "Google Japan G.K." and "グーグル合同会社" find-or-create three rows against a naive unique index. `agencies` carries that same trap today at a scale where it never bites; a table fed by every application creation is not that scale. Normalise to a canonical key on create and give the user a merge, or the table becomes the free-text field it replaced, wearing a join.
  - **Manual entry is honest here in a way it is not per-application.** The window and the language situation are typed once per employer and read by every application to it, so the fifth application to the same company costs nothing. That amortisation is what the field admission test is really asking about, and it is why these two facts would fail the test as `applications` columns and pass it as `companies` columns.
  - **Versioning:** swapping `company` for `company_id` is a backfill plus a backward-incompatible migration, free only in a major, so it belongs in the same pass as the schema debt below rather than being paid for twice.

- [ ] **Let the user edit the ghost-risk thresholds from the settings page** *(wanted 2026-08-03; `api/app/queries/applications/ghost_risk_query.rb`, `web/app/[locale]/(app)/settings/`)*. The 2026-08-03 fix took the half of this that was a fix: silence is counted in business days now, and the derived p90 is gone in favour of a stated policy, `THRESHOLDS` of 15 business days on `applied` and 10 on `phone_screen`. What is left is the genuinely new part, editing those two numbers without a deploy. **A capability, and a smaller one than before:** the constant is already the whole mechanism, so this is a `users` migration for two nullable integer overrides, a `PATCH /me` widening beside the residence fields, a settings card next to residence and passkeys, and two message catalogs. Null means "use the constant", so the shipped policy stays the default rather than becoming a fallback. **Keep a clamp on the manual path** (the removed one was 7 to 90 calendar days): a typo'd 1-day threshold flags the entire board, and the reason the old floor existed survives its cause.

- [ ] **Model "residents of Japan only" as its own fact, and filter on it** *(wanted 2026-08-03; the other half of the `v1` prefill bug above)*. That fix only stops the extractor lying; it leaves the residency requirement recorded nowhere. For this user it is the most useful filter on the board, and it points the *opposite* way to how the app treats it today: as a dependent already living in Tokyo he competes in a candidate pool less than half the size of the one for postings open to overseas applicants, so residents-only listings are where his odds are best and should be surfaced, not buried. **Not a fourth `sponsorship` value:** the axes are orthogonal (a residents-only role may or may not sponsor; a role open to overseas candidates may sponsor nothing), so folding them re-creates the collapse the `v1` fix removes, one layer down, and `hiring_entity`'s `unsupported` is a third axis again (can the company employ someone in Japan at all). So: a new per-application column plus a filter, a capability and an additive widening of the index contract. It **passes the field admission test** through the prefill door, since postings state residency plainly in the sentence the extractor already reads. Land it with the schema pass below, so the column starts tight.

- [ ] **Career intelligence: benchmark reports from published surveys** *(its headline view, comp percentile for the current position, needs `positions`. The offer-comp-vs-median slice became buildable when `v1.8.0` landed the 年収 structure, but stays bundled here: no annual refresh-cost commitment before `positions` makes the full view buildable.)* An in-app, yearly-refreshed summary of reliable market sources: the [TokyoDev annual survey](https://www.tokyodev.com/articles/the-2025-tokyodev-developer-survey-results-are-live) first (2025: 989 respondents; median ¥9.5M, split by employer type, experience band, and language use), with MHLW's official wage statistics (賃金構造基本統計調査) as the government counterpart. The point is not the report (a static summary page would fail the career-growth admission test) but the user's own data shown against it: comp percentile for the current position (needs `positions`), offer comp vs the market median for his experience band at decision time, his Japanese level against the language-requirement distribution of the roles he tracks. Implementation is a versioned reference dataset (a seed file refreshed once per survey cycle, no scraping, no pipeline), so the recurring cost is one data-entry session a year. Check each source's licensing and attribution terms before shipping its numbers.
- [ ] **Settle the `v1.x` schema debt in the first major: re-optimize every table change the minor releases were forced to leave loose** *(stays standalone in this cluster; rides whichever major actually ships, because a major is the only release where a backward-incompatible migration is free)*. The nullable-or-defaulted standing rule is a deliberate trade, minor-release safety then for schema looseness now, and this is where the bill gets settled. **The freeze fixes the size of that bill:** no future `v1` release adds a column, so what needs tightening is exactly what shipped through `v1.11.0`. The pass, table by table (`credentials` and `push_subscriptions` from `v1.6.0`, the eight nullable `applications` columns from `v1.8.0`, the visa/hiring/timezone fields from `v1.9.0`): backfill and tighten to `NOT NULL` or a real default wherever the domain requires the value, promote free-text columns to check-constrained enums where the value set has proven closed, add the indexes deferred on growth grounds, and drop any column the field admission test killed in use, since a field the user stopped filling is dead schema and the major is the one chance to delete it cheaply. **The `timeline_entries` offer-lookup index is off this list: it shipped on 2026-08-05** and is no longer owed here. It was parked for the major on the grounds that a personal-scale timeline is a few hundred rows, which was the wrong reading of why the read was slow. The cost was never the row count: `avg_days_to_offer`'s `DISTINCT ON` carried no user predicate, so a per-user endpoint scanned and sorted **every** account's rows, and the stats cache keys on `MAX(updated_at)`, so any edit to any application sent the next dashboard back through it. Scoping the subquery was the fix; `index_timeline_entries_on_offer`, partial on `(application_id, created_at) WHERE to_status = 'offer'`, is what serves it (`SPEC.md` § `timeline_entries`). A plain index on `to_status` is still not wanted, for the reason it never was: `'offer'` is the only value any query names. One exception so the pass does not over-tighten: sponsorship stays nullable by design, because unknown is decision-relevant signal, not missing data (`v1.9.0`'s admission-test note is the record). Since the Hono rewrite comes first, the tightening applies under the TS query layer rather than ActiveRecord, unchanged in substance: a loose schema under a new runtime is the same debt wearing a different API.
- [ ] **Rirekisho + shokumu-keirekisho generation** *(deliberately parked past the rest of the cluster; ship both together, after `positions`)*. Japanese applications conventionally want two documents: 履歴書 (personal history, a standardised form) and 職務経歴書 (career history, free-form). Generating both as PDFs from stored profile data would be the clearest possible signal that the author understands the market. 履歴書 alone is additive, but its twin *is* a `positions` table rendered as prose, and shipping half the pair, then maintaining a PDF toolchain for a year before the other half is buildable, is the worst version of the trade. **Format question answered (2026-07-11):** the JIS template was [withdrawn from the JIS standards in July 2020](https://japan-dev.com/blog/japanese-resume-rirekisho) (stationery stores still sell "JIS-compliant" pads, but it is a dead standard), and MHLW published a recommended replacement (厚生労働省様式) designed for fair hiring: [gender optional; commute time, dependents, and spouse fields removed](https://www.gtalent.jp/blog/japanwork-en/job-hunting-en/rirekisho-en). **Target the MHLW format**: it is the closest thing to official, and its fair-hiring rationale is a good line in the README. **Carry cost (recorded 2026-07-11): the highest in this file.** PDF generation is a known maintenance sink (toolchain choice, CJK font embedding, layout drift), and on Cloudflare Workers it is further constrained to pure-JS/WASM libraries or an external rendering service, no native binary toolchain; the MHLW format itself can also churn. Highest wow, highest carry; scope it with both eyes open, the way the dark-mode decision (`notes/HISTORY.md` § Settled, not shipped) weighed its cost side. **Storage of the pair is parked with it:** typed document slots for uploading a 履歴書/職務経歴書 alongside the English resume were considered for `v1.x` and deferred, being more blobs on the pg_dump-backed database (the cost the no-version-history decision refused) for documents whose real payoff arrives when generation ships. If uploads are wanted sooner, the shape is a typed `documents` table reusing the `v1.4.4` filename machinery, not more bytea columns.

- [ ] **Observability as the portfolio piece: Prometheus + Grafana, app metrics rather than container graphs** *(wanted 2026-08-21)*. **The one item in this cluster whose clock runs backwards, and worth reading before the rest of the entry.** Everything else here pays off after the search closes, which is what the trigger encodes; this pays off only *during* it, because its whole argument is hiring signal, and the rewrite retires that argument by name ("the app stops being a portfolio piece built to impress screeners"). The thing it would watch does not survive the trigger either: `docker-compose.prod.yml` is deleted wholesale by the move to Workers, so a Grafana stack built against it is bullet one of the freeze, work with a scheduled deletion date. It is filed here because the rule says a new idea gets written down here rather than argued into a patch, not because the trigger fits. **So it is build-it-now-as-a-fourth-knowing-exception or never**, and that is the decision to make, ahead of any implementation detail below.
  - **The half that fails the admission test is small, and is also the entire point.** A Rails `/metrics` endpoint (`prometheus-client`, or `yabeda-rails` + `yabeda-puma`) is a new capability, plainly. The compose-level half (Prometheus, Grafana, a collector) adds no application capability and would pass the test the way a CI chore does, but it is the half worth least: container CPU and memory graphs are commodity, and a screener has seen them on every other candidate's repo. The differentiator is domain metrics, meaning p95 per endpoint, Solid Queue depth and job latency, and applications by FSM state, a panel that could only have come from this app. Shipping only the half that passes the test is therefore the worst version of this item, not the safe one.
  - **cAdvisor works on this host now, but it is still the half worth least.** It did not work while the machine ran Docker Desktop for Linux (verified 2026-08-21): `/var/lib/docker` and `/var/run/docker.sock` did not exist on the host, only inside the VM, and its host-filesystem mounts resolved to the wrong place. The host moved to native Docker Engine on 2026-09-01, so both paths exist now and cAdvisor's standard mounts would resolve correctly. That was always a property of the container runtime and not of this app, the same shape as the `ufw` finding in `SPEC.md` § Network exposure, and it changed exactly as that finding predicted it would. Running cAdvisor is still not worth it: per-container CPU and memory graphs are the commodity half a screener has seen on every other repo. If some container-level metric is genuinely wanted, OpenTelemetry Collector's `docker_stats` receiver or Prometheus's Docker service discovery is the lighter way to get it.
  - **It must not open a listener, and by default it would.** `SPEC.md` § Network exposure records that no service publishes a host port and that `ss -tlpn` returns nothing on a routable interface. Grafana needs a UI, so binding it to `127.0.0.1` keeps that property literally true and is the default answer. A third tunnel hostname is the alternative and is only worth it for the reason in the next bullet, in which case it needs Cloudflare Access in front of it: the two existing hostnames are public by design and a Grafana login page is not the third thing to add to that list.
  - **A dashboard nobody can reach is not a portfolio piece.** The value exists only if a screener meets it inside the 90 seconds they spend: either screenshots committed into `README.md` and `README.ja.md` (cheap, and both already carry demo videos, so the machinery exists), or Grafana Cloud's free tier with a public dashboard link, which also moves the TSDB off this machine and drops the local stack to a single agent container. Decide which before building, because it changes what gets built.
  - **Cardinality is the disk cost, and it is the one number worth pre-committing.** cAdvisor-class collection emits on the order of a thousand series per container; at four containers and a 15s scrape that is roughly 3 GB/month of TSDB for series nobody will read. `metric_relabel_configs` dropping the noisy families is part of the initial config, not later tuning.
  - **Carry cost: three more images to keep current.** `cloudflared` is the cautionary precedent: it ran as `:latest` for a while, an unpinned image whose running version nobody had recorded, terminating every request, until it was pinned to `2026.8.2` (`SPEC.md` § Deployment). Pin Prometheus and Grafana to digests at the first `up`, or this item repeats that mistake three more times.
  - **The story it would tell has already happened, which is the strongest argument for building it.** `d7e4330` raised the `api` container's `mem_limit` from 512m to 768m after a measurement found it pinned at its ceiling: `anon` 493 MiB against a 512 MiB cap, page cache fully evicted, `memory.events` `max` at 92, and zero OOM kills and zero restarts, so nothing had ever surfaced it. That is a dashboard's entire value proposition demonstrated on this app and found by hand instead. It is also the caption for the screenshot.
