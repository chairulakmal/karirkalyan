# local research notes — not for the repo
/notes
/design
/.claude

# Secrets
.env
.env.local
.env*.local

# macOS
.DS_Store

# demo recordings — uploaded via the GitHub editor, never committed
*.webm
*.mp4