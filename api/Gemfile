source "https://rubygems.org"

gem "rails", "~> 8.1.3"
gem "pg", "~> 1.1"
gem "puma", ">= 5.0"
gem "bootsnap", require: false
gem "thruster", require: false
gem "tzinfo-data", platforms: %i[ windows jruby ]
gem "rack-cors"
gem "rack-attack"

# Auth
gem "devise"
gem "devise-jwt"

# Background jobs — disabled. Uncomment + run `bundle install` to re-enable
# (also see CLAUDE.md "Re-enabling Sidekiq").
# gem "sidekiq"
# gem "sidekiq-cron"
# gem "redis"  # required by :redis_cache_store and Sidekiq

# API docs — needed in production to serve /api-docs
gem "rswag-api"
gem "rswag-ui"

# Structured single-line JSON logs in production for Railway log aggregation
gem "lograge"

# AI job-URL pre-fill — extracts company/role/notes from a pasted posting URL.
# Official Anthropic SDK; key from ANTHROPIC_API_KEY (no-ops/raises cleanly if unset).
gem "anthropic", "~> 1.44"

group :development, :test do
  gem "dotenv-rails"
  gem "debug", platforms: %i[ mri windows ], require: "debug/prelude"
  gem "bundler-audit", require: false
  gem "brakeman", require: false
  gem "rubocop-rails-omakase", require: false
  gem "rspec-rails"
  gem "factory_bot_rails"
  gem "faker"
end

group :test do
  gem "database_cleaner-active_record"
  gem "rswag-specs"
  gem "simplecov", require: false
  gem "prosopite"
  gem "pg_query"
end

gem "honeybadger", "~> 6.6"
