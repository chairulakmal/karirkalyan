# Dependabot: DEACTIVATED 2026-07-22, temporarily.
#
# Shipped in v1.11.0, its first monthly run opened nine PRs at once (a grouped
# api + web pair, four majors held out of the groups, and three Actions bumps),
# which is exactly the required-check and Railway-deploy noise the config was
# meant to avoid. Paused until we choose to turn it back on; the majors it
# surfaced (typescript 7, eslint 10) need code work to adopt and are tracked
# separately, not by this file.
#
# To REACTIVATE: uncomment the block below (it is the original v1.11.0 config,
# unchanged). Nothing else is needed.

# version: 2
# updates:
#   - package-ecosystem: bundler
#     directory: /api
#     schedule:
#       interval: monthly
#     open-pull-requests-limit: 5
#     groups:
#       api-minor-patch:
#         update-types:
#           - minor
#           - patch
#
#   - package-ecosystem: npm
#     directory: /web
#     schedule:
#       interval: monthly
#     open-pull-requests-limit: 5
#     groups:
#       web-minor-patch:
#         update-types:
#           - minor
#           - patch
#
#   - package-ecosystem: github-actions
#     directory: /
#     schedule:
#       interval: monthly
#     groups:
#       actions:
#         update-types:
#           - minor
#           - patch
